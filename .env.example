# Runtime environment: production | dev
APP_ENV=production

# Optional runtime logging overrides are configured in config/config.yaml:
# logging.level=info
# logging.db-level=warn
# access-log.redaction-mode=partial

# Build-time paths — must match frontend Vite build (changing requires rebuild)
ROOT_PATH=/wall
ADMIN_PATH=/admin
GUARD_PATH=/guard
